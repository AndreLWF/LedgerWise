# Teller certificates (never commit — they grant bank access)
backend/certs/

# Backend
backend/.env
backend/__pycache__/
backend/**/__pycache__/
backend/*.pyc
backend/.venv/
backend/venv/

# Frontend
frontend/.env
frontend/node_modules/
frontend/.expo/
frontend/dist/
frontend/web-build/

# Claude Code (local settings are personal, skills are shared)
.claude/settings.local.json

# Admin scripts (contain DB access patterns — keep local)
backend/scripts/

# Secrets & certificates (defense-in-depth)
*.pem
*.key
*.crt
*.p12
.env.local
.env.*.local

# General
.DS_Store
*.log
